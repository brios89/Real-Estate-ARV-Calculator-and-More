# Local testing only. In production these live in Vercel → Settings → Environment Variables.
RENTCAST_API_KEY=your_rentcast_key_here
# Optional: lock the proxy to your site's domain (leave blank for same-origin)
ALLOWED_ORIGIN=
